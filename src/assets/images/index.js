const images = {
    logo: require('./logo.svg').default,
    clear: require('./clear.svg').default,
    search: require('./search.svg').default,
    loading: require('./loading.svg').default,
    check: require('./check.svg').default,
    more: require('./more.svg').default,
    homePlay: require('./homePlay.svg').default,
    language: require('./language.svg').default,
    question: require('./question.svg').default,
    darkMode: require('./darkMode.svg').default,
};

export default images;
